export const enum InputClasses {
  DIALED = 'dialed-input',
  CHECKED = 'checked',
  FILE = 'file'
}

export const enum SelectClasses {
  COUNTRY = 'country-select',
}

export const enum ButtonClasses {}
